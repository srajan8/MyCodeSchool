#include "base.h"

int main() {
    clear();

    // Prime number - O(n) vs O(logn)

    // Running Time depends on
    /*
        Processor - Single or Multi
        Memory R/W speed
        32 or 64 bit OS
        Input
    */

   /*
    function(A, n) {
        int total = 0; =1
        for(i<n) { =2*(n+1)
            total += i; =2*n
        }
        return total; =1
    }
   */ // 1 + 2n + 2 +2n +1 = 4n+4

    // O(1) vs O(n) vs O(n^2)
    // Asymtotic Notations to represent f(n)
    /*
        BigOh - upper bound, worst case
            f(n)<=c*g(n), O(g(n))
        BigOmega - lower bound, best case
            c*g(n)<=f(n), Ω(g(n))
        BigTheta - middle bound, normal case
            c1*g(n)<=f(n)<=c2*g(n), θ(g(n))
    */

    // Running Time = Sum of all Time of fragments
    // Simple Statements O(1), 
    // Single Loop O(n), 
    // Double Nested Loop O(n^2).
    




    return 0;
}